{
  "scripts": {
    "start": "webpack --progress --colors --watch",
    "test": "eslint --cache *.js src && echo ✓",
    "compile": "webpack"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "es6-transpiler-config": "github:FND/es6-transpiler-config",
    "eslint-config-fnd": "github:FND/eslint-config"
  }
}
